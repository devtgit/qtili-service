{
  "name": "qtili-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "emulators": "firebase emulators:start --import \"./tmp-imported-firestore-data\" --export-on-exit --only \"functions,firestore,auth,storage\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "@types/node": "^20.3.1"
  }
}
